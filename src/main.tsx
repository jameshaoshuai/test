import React from 'react';
import ReactDOM from 'react-dom/client';
import NpcSinglePanelPrototype from './NpcSinglePanelPrototype';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NpcSinglePanelPrototype />
  </React.StrictMode>,
);
